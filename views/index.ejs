<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <h1>Tweets</h1>

    <div class="posts-row">
        <% for (let tweet of tweets) { %>
            <div class="post">
                <h3 class="user">@<%= tweet.username %></h3>
                <h4 class="content"><%= tweet.content %></h4>

                <a href="/tweets/<%= tweet.id %>">See detail</a>
                <br/><br/>
                <a href="/tweets/<%= tweet.id %>/edit">Edit tweet</a>
                <br/><br/>
                <form method="post" action="/tweets/<%= tweet.id %>?_method=DELETE">
                    <button>Delete tweet</button>
                </form>
            </div>
        <% } %>
    </div>

    <a href="/tweets/new">New tweet</a>
</body>
</html>
